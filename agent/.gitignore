# Build output
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
x64/
ARM64/

# User-specific files
*.user
*.suo
*.userosscache

# Generated WinUI 3 files
**/Generated Files/
*.tlog
*.pdb

# Rider / VS local
.vs/
.idea/

# Velopack pack output (Setup.exe + delta/full nupkg + RELEASES feed)
artifacts/

# Local-only config overrides (tenant/client IDs, login hints, etc.)
**/appsettings.Development.json

# Test output: trx logs + the visual e2e's captured overlay/toast PNGs
# (agent/tests/FocusAgent.IntegrationTests/TestResults/). CI uploads these as a
# run artifact; they must never be committed.
**/TestResults/
